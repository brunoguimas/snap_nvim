return {
        'rmagatti/auto-session',
        lazy = false,

        opts = {
                auto_restore = true,
                auto_session_ignore = { "neo-tree", "neo-tree-popup", "fugitive", "gitcommit" }
        }
}
